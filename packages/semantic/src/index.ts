// packages/semantic — YAML metric catalog + query planner/executor (ADR-010,
// E10). The LLM outputs a structured plan validated against the catalog and
// never produces raw SQL (E10-S2). Lands with E10 (M2 milestone).
export {};
