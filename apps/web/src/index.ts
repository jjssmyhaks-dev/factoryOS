// apps/web — Next.js (App Router) + Tailwind + shadcn/ui customer console.
// Implemented in E12 (approvals inbox, AI Activity ledger, autonomy settings,
// ROI report, connection wizard). Deployed serverlessly via OpenNext (ADR-013).
export {};
