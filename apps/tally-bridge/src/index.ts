// apps/tally-bridge — Node/TypeScript Windows service next to TallyPrime
// (ADR-004), outbound-only MQTT to AWS IoT Core (ADR-014), pairing by device
// code, IoT Jobs OTA. Implemented in E4.
export {};
